import "./satelliteChart.scss";
import React, { useEffect, useState } from 'react';

const SatelliteChart = ({nmsCurrent}) => {

    useEffect(() => {
    }, nmsCurrent);

    // History에서 받아온 nmsCurrent Data로 전체 Device SatCnr을 수집하여 Chart생성 -> 위성신호 알 수 있음
    return(
        <>
            -----
        </>
    )
}

export default SatelliteChart;